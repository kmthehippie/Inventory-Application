<!DOCTYPE html>
<html lang="en">
<%- include('./partials/header') %>
<body>
  <div class="container-fluid">
    <div class="row">
        <div class="col-sm-2">
            <%- include('partials/sidebar') %>
        </div>
        <div class="col-sm-10">
            <h1>Spoilage ID:</h1>
            <p>  <%= title %> </p>

            <h3><%= fruit.name %></h3>
            <table>
                <tr>
                    <td class="p-3">Date Spoiled</td>
                    <td><%= spoilage.spoilage_date_formatted %></td>
                </tr>
                <tr>
                    <td class="p-3">Amount Spoiled</td>
                    <td><%= spoilage.amount %> <%= fruit_instance.quantitytype %></td>
                </tr>
            </table>
            <hr>
            <a href="<%= fruit_instance.url %>"><button class="btn btn-primary m-3">Back to Fruit Instance</button></a>
            <!-- Need to fix this so that data uploaded to server can be displayed... -->
            <a href="<%= spoilage.url%>/delete"><button class="btn btn-primary m-3">Delete Spoilage</button></a>
            <a href="<%= spoilage.url%>/update"><button class="btn btn-primary m-3">Update Spoilage</button></a>
            <hr>
            <p>Proof of Spoilt Fruit </p>
            <%spoilage.imgurls.forEach(img =>{ %>
            <img src="<%= img %>" alt="Placeholder image" width="500" height="300" style="object-fit:contain">
            <br/><br/>
            <% }) %>
            
           
            
        </div>
    </div>
</div>
</body>
</html>